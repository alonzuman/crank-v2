<%= render  "shared/top_bar", center: 'New Post', left: (link_to 'Back', :back) %>
<%= simple_form_for(@post) do |f|%>
<%= f.input :description %>
<%= f.button :submit, class:'btn btn-primary' %>
<% end %>